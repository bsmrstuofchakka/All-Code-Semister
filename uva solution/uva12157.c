#include<stdio.h>
int main()
{
    int i,j,t,n;
    long long int a,b,sum1,sum2,sum;
    scanf("%d",&t);
    for(i=1; i<=t; i++)
    {
        sum=0;
        scanf("%d",&n);
        for(j=1; j<=n; j++)
        {
            scanf("%d",&a);
            while(a>0&&a<99)
            {
                a=a/10;
            }
            while()
            if(a==1||a==2)
                sum=sum+10;
             else if(a==3&&a==4)
                    sum=sum+2*10;
             else if(a==5&&a==6)
                sum=sum+3*10;
             else if(a==)
        }
    }



    return 0;
}
